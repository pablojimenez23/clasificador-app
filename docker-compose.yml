version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./api/modelo:/app/modelo

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - api